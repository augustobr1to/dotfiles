# Delay installs of newly published versions (requires npm 11.10+)
min-release-age=7

# Block postinstall/preinstall scripts from running automatically
ignore-scripts=true

# Enforce lockfile integrity — fail if package-lock.json is missing or outdated
package-lock=true

# Enforce engine version requirements (pair with package.json "engines" field)
engine-strict=true
